import React from "react";
import {
  FlexScreenContainer,
  HeaderText,
  ButtonRow,
  Button,
  ButtonText,
  KeyboardView
} from "../../components/common";

const ChannelScreen = props => {
  return (
    <FlexScreenContainer>
      <HeaderText>Channel Screen</HeaderText>
    </FlexScreenContainer>
  );
};

export default ChannelScreen;
