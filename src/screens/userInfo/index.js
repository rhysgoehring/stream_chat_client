import React from "react";
import {
  FlexScreenContainer,
  HeaderText,
  ButtonRow,
  Button,
  ButtonText,
  KeyboardView
} from "../../components/common";

const UserInfoScreen = props => {
  return (
    <FlexScreenContainer>
      <HeaderText>User Info Screen</HeaderText>
    </FlexScreenContainer>
  );
};

export default UserInfoScreen;
